# pico-matrix-scan

This folder contains the code to flash onto a Pico connected to the reed switches.

It reads the matrix of reed switches and outputs to the Pi over Serial (USB).

You only need to copy `main.py` and `keypad_lib.py`.
